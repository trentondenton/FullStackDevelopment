export function multiply(numOne, numTwo) {
  return numOne * numTwo;
}

export function divide(numOne, numTwo) {
  return numOne / numTwo;
}

export default function (numOne, numTwo) {
  return numOne + numTwo;
}